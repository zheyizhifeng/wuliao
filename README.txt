代码经过调整和优化后结构重整如下：
有效文件只有3个：
index.php-------所有首页表内容展示（包括直接分页，搜索结果，添加结果，更新结果）
link.php--------用于连接数据库和确定字符集编码为UTF-8
manage.js-------用于前后端数据的交换（如无刷新更新更改后的数据

另外两个文件
bootstrap.css和jquery.js都是第三方库文件,由于我引入了网络CDN加速的地址，所以没用上这两个文件;

具体文件的结构见详细注释。
